# Fish-Agent v2.5 前端交互与视觉优化

> 项目路径：`D:\1_Backend\Fish-Agent`  
> 覆盖版本：**v2.5**（在 [v2.4](v2.4-记忆链路与可靠性增强-20260506.md) 基线上完成：**新会话发送阻塞修复**、**禁用按钮暗色一致性修复**、**全局滚动条主题化**、**聊天页视觉与交互细节优化**）。  
> 文档生成日期：**2026-05-06**

**分文档索引**

- [v2.0-全新版本-20260504.md](v2.0-全新版本-20260504.md) — v2.0 架构与接口基线  
- [v2.1-知识库闭环与管理页-20260505.md](v2.1-知识库闭环与管理页-20260505.md) — v2.1 增量记录  
- [v2.2-DeepSeek对话接入-20260506.md](v2.2-DeepSeek对话接入-20260506.md) — v2.2 增量记录  
- [v2.3-Redis对话限流-20260506.md](v2.3-Redis对话限流-20260506.md) — v2.3 增量记录  
- [v2.4-记忆链路与可靠性增强-20260506.md](v2.4-记忆链路与可靠性增强-20260506.md) — v2.4 增量记录  
- [v2-全景文档-20260505.md](v2-全景文档-20260505.md) — v2 全景（含 v2.5 更新）

---

## 一、v2.5 一句话增量

1. **修复新会话首条消息 UI 阻塞**：`chat.ts` 在新会话发送时立即切换到 `__pending__`，首条用户消息和助手占位气泡可即时渲染，不再卡在欢迎页。  
2. **修复暗色主题下 disabled 按钮发白**：覆盖 Element Plus `plain` 按钮 disabled 变量，保持品牌色与主题一致，仅降低透明度。  
3. **统一滚动条视觉**：新增 WebKit + Firefox 全局滚动条样式，替换 Windows 默认宽滚动条。  
4. **聊天页微交互升级**：欢迎建议卡片样式增强、消息区手动平滑回底、用户气泡阴影、侧栏 hover 位移、发送按钮按压反馈。

---

## 二、问题与修复

### 2.1 新会话首条消息阻塞（Bug 1）

**问题现象**：新建会话后发送首条消息，短时间内仍显示欢迎页，直到后端返回 `session` 事件才出现消息。  

**根因**：消息被写到 `messagesBySid['__pending__']`，但 `messages` 计算属性读取的是 `messagesBySid[activeSid.value]`，当 `activeSid=''` 时读取不到新消息。  

**修复要点**（`frontend/src/store/chat.ts`）：
- 发送入口保存 `apiSid`（原始 sid）并在新会话时立即设 `activeSid='__pending__'`  
- `assignedSid` 初始化改为 `apiSid`，保持 `onSession` 中 `if (!assignedSid)` 语义正确  
- `onChunk` / `onTool` 直接使用 `activeSid.value`  
- `finally` 增加兜底：若始终未拿到真实 sid，清理 `__pending__` 并重置 `activeSid=''`

---

### 2.2 disabled 按钮暗色发白（Bug 2）

**问题现象**：流式生成中，`plain` 按钮禁用后背景偏白，与暗色主题冲突。  

**修复要点**：
- `SessionSidebar.vue`：`新会话` 按钮增加 `.is-disabled` 明确变量覆盖  
- `KnowledgeUpload.vue`：分别覆盖 `primary plain` 与 `default plain` 两类 disabled 样式  
- 统一策略：`background=transparent`，边框/文字跟随主题变量，`opacity=0.5`

---

### 2.3 滚动条默认样式不统一（Bug 3）

**修复文件**：`frontend/src/style.css`  

新增：
- `::-webkit-scrollbar` / `::-webkit-scrollbar-thumb` / `::-webkit-scrollbar-track`  
- Firefox `scrollbar-width: thin` + `scrollbar-color`

---

### 2.4 页面美化与交互细节（Bug 4）

**MessageList.vue**
- `scrollToBottom(force=true)` 使用 `scrollTo(..., behavior:'smooth')`
- 自动追随输出仍保留 `scrollTop` 直达，避免流式渲染抖动
- 欢迎建议卡片新增左侧 accent 线与更大圆角

**MessageBubble.vue**
- 用户气泡增加轻阴影，暗色模式下阴影参数增强

**SessionSidebar.vue**
- 列表项过渡扩展为 `background + transform`
- hover 增加 `translateX(2px)` 轻反馈

**ChatInput.vue**
- 发送按钮按压态缩放（`:active`）

---

## 三、变更文件清单

| 文件 | 变更性质 | 说明 |
|------|----------|------|
| `frontend/src/store/chat.ts` | 修改 | 新会话 `__pending__` 逻辑修复与失败兜底 |
| `frontend/src/components/SessionSidebar.vue` | 修改 | 新会话 disabled 样式覆盖 + 列表 hover 过渡优化 |
| `frontend/src/components/KnowledgeUpload.vue` | 修改 | 上传按钮 disabled 样式覆盖 |
| `frontend/src/style.css` | 修改 | 全局滚动条样式（WebKit + Firefox） |
| `frontend/src/components/MessageList.vue` | 修改 | 手动回底平滑滚动 + 欢迎卡片样式增强 |
| `frontend/src/components/MessageBubble.vue` | 修改 | 用户气泡微阴影（含暗色适配） |
| `frontend/src/components/ChatInput.vue` | 修改 | 发送按钮按压缩放动效 |

---

## 四、体验收益

- **首条消息可见性**：新会话首发即时显示，交互反馈显著改善  
- **暗色一致性**：禁用态按钮不再出现突兀亮底  
- **视觉统一**：滚动条风格与主题变量对齐，跨页面统一  
- **微交互质量**：卡片、气泡、列表、发送按钮反馈更自然

---

_文档版本：**v2.5 前端交互与视觉优化** · 生成日期：2026-05-06 · 承接 **v2.4**_
